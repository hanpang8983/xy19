<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 读取属性文件 
    <properties resource="jdbc.properties"/>
    -->
    <!-- 连接数据库的信息 
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}" />
                <property name="url" value="${jdbc.url}" />
                <property name="username" value="${jdbc.username}" />
                <property name="password" value="${jdbc.password}" />
            </dataSource>
        </environment>
    </environments>
    -->
    <!--  
    <mappers>
        
        <mapper resource="com/hanpang/framework/rbac/role/model/RoleMapper.xml"/>
        <mapper resource="com/hanpang/framework/rbac/user/model/UserMapper.xml"/>
        <mapper resource="com/hanpang/framework/rbac/menu/model/MenuMapper.xml"/>
    </mappers>
    -->
</configuration>